<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary">

        <EditText
            android:id="@+id/fragment_home_list_search"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginEnd="14dp"
            android:layout_marginStart="14dp"
            android:layout_marginTop="30dp"
            android:background="@drawable/drawable_rectangle_grey"
            android:lines="1"
            android:padding="5dp"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/fragment_home_list_search_icon"
            android:layout_width="17dp"
            android:layout_height="17dp"
            android:layout_marginEnd="20dp"
            android:src="@drawable/icon_search"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/fragment_home_list_search"
            app:layout_constraintEnd_toEndOf="@id/fragment_home_list_search"
            app:layout_constraintTop_toTopOf="@+id/fragment_home_list_search" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/fragment_home_list"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/fragment_home_list_search" />
    </android.support.constraint.ConstraintLayout>
</layout>
